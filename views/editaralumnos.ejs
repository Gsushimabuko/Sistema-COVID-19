                        <!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.4.1/css/bootstrap.min.css" integrity="sha384-Vkoo8x4CGsO3+Hhxv8T/Q5PaXtkKtu6ug5TOeNV6gBiFeWPGFN9MuhOf23Q9Ifjh" crossorigin="anonymous">
    <title>Gestionar personas</title>
    <link rel="stylesheet" type="text/css" href="/estilos/header.css">
</head>
<body class="container">
    <%- include('menu') %>
    <div class="header1" id="bajar">
        
        <h2><b>Editar Alumno</b></h2>
    </div>

    <form method="post" action="/editaralumnos/<%= alumno.id %>">

        <div>
            <label class="form-label">Sección:</label>
            <select name="grupo" class="form-select mb-2">
                <option value="all">Elija la sección</option>
                
                <% salones.forEach( (i)=> { %>
                    
                    <% if(i.id == alumno.grupoId){ %>
                        <option selected value="<%= i.id %>"> <%= i.nombre %> </option>
                   <% }else{ %>

                    <option value="<%= i.id %>"> <%= i.nombre %> </option>
                    
                    <% } %>
                    
                <% }) %>
    
            </select>

        </div>
        
        <label class="form-label">DNI:</label>
        <input name="dni" type="text" class="form-control" value="<%= alumno.dni %>"/>

        <label class="form-label">Nombre:</label>
        <input name="nombre" type="text" class="form-control" value="<%= alumno.nombre %>"/>

        <label class="form-label">Apellido paterno:</label>
        <input name="apellidop" type="text" class="form-control" value="<%= alumno.apellidop %>"/>
            
        <label class="form-label">Apellido materno:</label>
        <input name="apellidom" type="text" class="form-control" value="<%= alumno.apellidom %>" />
            

        <label class="form-label">Teléfono:</label>
        <input name="telefono" type="text" class="form-control"  value="<%= alumno.telefono %>"   />
            
        <button type="submit" class="btn btn-warning mt-2">Guardar</button>
        
    </form>


    <div class="card mt-2">

        <div class="card-header">
            <b>Padres</b>
        </div>
        <div class="card-body">
            <div>
                <b>
                    Padre:
                    <%= padre.nombre %> <%= padre.apellidop %>
                </b>
            </div>
            <div>
                <b>
                    Madre:
                    <%= madre.nombre %> <%= madre.apellidop %>
                </b>
            </div>

        </div>
        
        <div class="mt-2 mb-2 ml-2 mr-2">
            <a class="btn-sm btn-info mt-2" href="/editarpadres/<%= alumno.dni %>"> Editar padre o madre </a>

        </div>
    </div>

    
    <%- include('footer') %>

</body>
</html>